# Part 2 — String Methods & Transformations

## Topics Covered
- `upper()`
- `lower()`
- `capitalize()`
- `title()`
- `swapcase()`
- `casefold()`
- `replace()`
- `strip()`
- `lstrip()`
- `rstrip()`
- `center()`
- `ljust()`
- `rjust()`
- `zfill()`
- Method chaining

---

## Practice Overview

In this section, I practiced built-in Python string methods used for text formatting, cleaning, and transformation.

### Key Concepts Practiced
- Converting text into different cases
- Replacing portions of strings
- Removing unnecessary spaces
- Aligning text using justification methods
- Formatting outputs for better readability
- Applying multiple methods together using method chaining

---

## Learning Outcome
Through these exercises, I learned:
- Efficient string transformation techniques
- Text formatting and cleaning operations
- Practical use of built-in string methods
- Writing cleaner and more readable string manipulation code

These concepts are essential for real-world text processing and data cleaning tasks.