# Part 3 — Searching, Checking & Manipulation

## Topics Covered
- `find()`
- `rfind()`
- `index()`
- `rindex()`
- `count()`
- `startswith()`
- `endswith()`
- `partition()`
- `rpartition()`
- `split()`
- `rsplit()`
- `join()`
- `removeprefix()`
- `removesuffix()`
- `isdigit()`
- `isalpha()`
- `isalnum()`
- `islower()`
- `isupper()`
- `isspace()`
- `istitle()`

---

## Practice Overview

In this section, I explored searching, validation, splitting, joining, and checking operations for strings in Python.

### Key Concepts Practiced
- Searching for characters and substrings
- Counting occurrences inside strings
- Splitting and joining text
- Prefix and suffix manipulation
- Checking string properties such as:
  - numeric
  - alphabetic
  - uppercase/lowercase
  - whitespace
  - title case

---

## Learning Outcome
Through these exercises, I developed a deeper understanding of:
- String searching techniques
- Text validation and checking
- Data cleaning and preprocessing basics
- Practical manipulation of text data

These operations are highly useful in input validation and data processing tasks.